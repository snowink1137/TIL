package jpabook.jpashop.service;

import lombok.Getter;
import lombok.Setter;

@Getter
@Setter
public class UpdateItemDto {
    // ItemServie 업데이트 로직 수행할 때, 파라미터 넘기는 게 많으면
    // DTO 이렇게 따로 만들어서 한번에 넘기는 것도 좋다
}
