# 20190122 workshop

```sqlite
create table bands (
	id integer,
    name text,
    debut integer
);

insert into bands (id, name, debut)
values
(1, 'Queen', 1973),
(1, 'Coldplay', 1998),
(1, 'MCR', 2001);

select id, name from bands;
select name from bands where debut < 2000;
```

